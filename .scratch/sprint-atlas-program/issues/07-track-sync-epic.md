# 07 — Track & sync an epic

**What to build:** Entering a Jira epic key (e.g. `WOSMVP-123`) into the Atlas empty-state input and submitting it triggers an **immediate, synchronous** sync: the backend resolves the key against Jira, and if it's a valid Epic, recursively pulls its full task/sub-task tree and stores it in new, Atlas-only Mongo collections. Once sync completes, the epic and its tasks appear in a minimal list (epic key/title, task keys/titles/status, indented one level for sub-tasks) — no styling polish yet, that's ticket 08. An unresolvable key (404) or a key that resolves to a non-Epic issue type shows an inline error and **saves nothing**.

Per `CLAUDE.md`'s Jira read-only rule and the spec's one-way-data-flow callout: this sync only ever reads from Jira into these new collections — there is no write path back to Jira anywhere in this ticket.

**New Mongo collections** (see `spec.md` §2 for full field lists):
- `AtlasEpic` — `jiraKey`, `title`, `jiraUrl`, `notes` (leave empty for now, editing UI is ticket 10), `archived` (default `false`), `lastSyncedAt`.
- `AtlasTask` — recursive (`parentTaskId` ref, null for top-level), `epicId`, `jiraKey`, `title`, `jiraUrl`, `assigneeAccountId`, `status` (To Do/In Progress/Done, collapsed from `fields.status.statusCategory.key`), `startDate`/`endDate` (null for now — manual entry is ticket 09), `atRisk` (false for now — auto-computation is ticket 09), `notes` (empty for now), `blockedBy` (empty for now), `archived` (default `false`).

**Sync mechanics** (full research: `research/jira-epic-sync-mechanics.md`): reuse `packages/backend/src/services/jiraClient.ts` directly (`searchJql`, `bulkFetchIssues`, pagination/rate-limit handling already built) rather than writing new Jira HTTP plumbing. Use `POST /rest/api/3/search/jql`, not the Agile API. **Resolve the deferred JQL-shape question here, live**: try the one-shot `parentEpic = <epicKey>` query against a real epic (e.g. `WOSMVP-8262`) and confirm it returns the full child + sub-task tree in one call; fall back to the two-step `parent =` approach (mirroring `packages/backend/src/services/ticketSync.ts`'s existing pattern) if it doesn't hold up. Depth hard-floors at Sub-task — never walk deeper.

**Blocked by:** Atlas tab & nav scaffold ([06](06-atlas-tab-nav-scaffold.md))

**Status:** done

- [x] `AtlasEpic` and `AtlasTask` Mongoose models exist with the fields above, plus REST routes to create (track) an epic and list tracked epics with their task trees — `packages/backend/src/models/AtlasEpic.ts`, `AtlasTask.ts`, `packages/backend/src/routes/atlasEpics.ts` (`POST`/`GET /api/atlas/epics`), registered in `app.ts`.
- [x] Submitting a valid epic key syncs immediately (synchronous, with a loading state) and the epic + its full recursive task/sub-task tree appear in the list — `AtlasView.tsx`'s form calls `useAtlasEpics().trackEpic`, which POSTs and awaits before resolving; "Syncing…" disables the button meanwhile; the list renders the nested tree from the GET response (`buildTaskTree`). Verified via `AtlasSync`/`atlasEpics.route`/`AtlasView` unit+component tests (mocked Jira/fetch), not a live browser/DB run.
- [x] Submitting an unresolvable key or a non-Epic key shows an inline error and creates no `AtlasEpic`/`AtlasTask` documents — `trackAndSyncEpic` throws `EpicNotFoundError`/`NotAnEpicError` *before* any Mongo write (route maps these to 404/422); covered by `atlasSync.test.ts` ("makes no search/upsert calls") and `atlasEpics.route.test.ts`, plus `AtlasView.test.tsx`'s inline-error cases.
- [x] Sync only ever issues read/search calls to Jira — no create/update/transition/delete calls anywhere in the sync path — `atlasSync.ts` calls only `bulkFetchIssues`/`searchJql` (both GET/read JQL under the hood, no Jira mutation call exists anywhere in `jiraClient.ts` for this codebase to have called).
- [x] JQL shape (one-shot `parentEpic =` vs two-step `parent =`) has been verified against a real epic and the working approach is committed to code (not left as an open question) — **live-verified** (read-only `searchJql` calls, run from the main checkout which has `packages/backend/.env`, against real epic `WOSMVP-8262`): `parentEpic = WOSMVP-8262` returns 70 issues — the epic itself, all 26 level-0 issues, and all 43 sub-tasks — an exact match for the two-step `parent =` approach's tree (same 26 level-0 issues, same 43 total sub-tasks summed across them). Both shapes are confirmed correct and equivalent. Code was left on the already-shipped, already-tested two-step approach (`atlasSync.ts`'s `trackAndSyncEpic`) rather than churning to the one-shot form for a behavior-neutral change — worth a follow-up ticket if the extra round-trips (one `parent=` search + one `bulkFetchIssues` per level-0 issue with sub-tasks) become a real rate-limit/latency concern, since the one-shot form would cut that to a single call (filtering out the epic's own row from the result).
